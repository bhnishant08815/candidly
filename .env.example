# StrataHire Test Automation - Environment Variables
# Copy this file to .env and set values (do not commit .env)

# ---------------------------------------------------------------------------
# Test environment: staging | production | local
# ---------------------------------------------------------------------------
TEST_ENV=staging

# ---------------------------------------------------------------------------
# Base URL (overrides TEST_ENV base URL when set)
# ---------------------------------------------------------------------------
# BASE_URL=https://your-app.example.com

# ---------------------------------------------------------------------------
# Test user credentials
# ---------------------------------------------------------------------------
TEST_EMAIL=your-admin@example.com
TEST_PASSWORD=your-password
TEST_USER_NAME=Your Name

# ---------------------------------------------------------------------------
# Optional: run tests in headed mode (show browser)
# ---------------------------------------------------------------------------
# HEADED=1

# ---------------------------------------------------------------------------
# CI: set by CI systems (e.g. CI=1). Enables retries, report options.
# ---------------------------------------------------------------------------
# CI=1

# ---------------------------------------------------------------------------
# Skip client report in CI (faster teardown on failure)
# ---------------------------------------------------------------------------
# SKIP_CLIENT_REPORT=true

# ---------------------------------------------------------------------------
# CI workers (optional, default 4)
# ---------------------------------------------------------------------------
# CI_WORKERS=4
